services:

  soc-dashboard:

    build:
      context: ./dashboard-app
      dockerfile: Dockerfile

    container_name: enterprise-soc-dashboard

    ports:
      - "8501:8501"

    volumes:
      - ./dashboard-app:/app

    working_dir: /app

    command: streamlit run app.py --server.address=0.0.0.0
